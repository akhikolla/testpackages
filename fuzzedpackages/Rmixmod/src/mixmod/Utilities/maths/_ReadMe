[bauder, 2013-06-01]

This (temporary?) folder will contain several interface functions to 
mathematical libraries, such as Eigen, Armadillo or Lapack.

Using a generic code elsewhere (with an intermediate type, see /Matrix directory)
allows to easily switch from one library to another.
