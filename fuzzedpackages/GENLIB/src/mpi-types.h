/* Type definitions generated by 'types.pl' */

//PETITE MMODIFICATION POUR S'ADAPTER A LA PLATEFORME UTILISE
#if defined _WIN32 || defined _WIN64
	//#define XLONG		unsigned _int64	 --JF
	#define XLONG		unsigned long long		 //--JF
	#define MAX_XLONG	_UI64_MAX

#else
	#define XLONG unsigned long long int
	#define MAX_XLONG	ULLONG_MAX
#endif

typedef char             mp_sign;
typedef unsigned int     mp_digit;  /* 2 byte type */
typedef XLONG 			mp_word;   /* 4 byte type */
//typedef unsigned short     mp_digit;  /* 2 byte type */
//typedef unsigned int       mp_word;   /* 4 byte type */

typedef unsigned int       mp_size;
typedef int                mp_err;

#define MP_DIGIT_BIT       (CHAR_BIT*sizeof(mp_digit))
#define MP_DIGIT_MAX       USHRT_MAX //ULONG_MAX : RADIX being MP_DIGIT_MAX+1, it became 0 and the function <s_mp_norm> made no sense.
#define MP_WORD_BIT        (CHAR_BIT*sizeof(mp_word))
#define MP_WORD_MAX        MAX_XLONG

#define RADIX              (MP_DIGIT_MAX+1)

#define MP_DIGIT_SIZE      2
#define DIGIT_FMT          "%10lX"
